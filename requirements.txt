# Core
python-dotenv

# FastAPI & Server
fastapi>=0.100.0,<0.136.0
uvicorn[standard]

# Pydantic
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.0.0

# AI Agent framework — OpenAI Agents SDK drives the agent loop and connects to the
# MCP tool server (ai-mcp-service) over streamable HTTP. Pulls in openai>=2 + httpx.
# (The MCP client lives in agents.mcp, so fastmcp is NOT needed here.)
openai-agents>=0.2.0

# Authentication
python-jose[cryptography]

# MongoDB — chat session history
motor
pymongo

# Co-located MCP tool server (vendored under ./mcp). Installs fastmcp, pinecone,
# asyncpg, yfinance, etc. so the MCP process can run in the same deployment.
-r mcp/requirements.txt
